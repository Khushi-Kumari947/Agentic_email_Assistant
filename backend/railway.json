{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn src.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "volumes": [
      {
        "name": "faiss-data",
        "mountPath": "/app/data"
      }
    ]
  }
}